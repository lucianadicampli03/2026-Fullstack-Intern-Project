# Local: leave unset — Vite proxies /api → http://localhost:3001 (see vite.config.js).
#
# Production (Vercel or Netlify): set this to your Render API URL, no trailing slash.
# Example: https://word-game-api-xxxx.onrender.com
VITE_API_URL=
